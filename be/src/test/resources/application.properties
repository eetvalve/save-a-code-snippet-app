spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=sa


## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# 128 bit key
crypto.key = ${CRYPTO_KEY:Bar12345Bar12345}

secure.code = ${SECURE_CODE:koodi}

super.user = ${SUPER_USER:edu}

server.port = 8081

endpoints.cors.allowed-origins=http://localhost:8080

spring.mvc.dispatch-options-request=true
        
spring.cloud.gcp.storage.credentials.location= file:///D:/keys/create-a-code-snippet-7e367a8fc675.json

location.location = "hello"